# melt-binaries — mac-min

Minimal working set of `melt` for macOS — just enough to render an `.mlt` project to `.mp4` headlessly. See `main` branch for how this was determined.

```bash
git clone --branch mac-min --single-branch --depth 1 https://github.com/joseluisbz/melt-binaries.git
```
