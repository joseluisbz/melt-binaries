# melt-binaries — mac-full

Complete decompressed `shotcut-macos-26.8.1.dmg` (Shotcut.app/Contents) — melt is at `MacOS/melt`. See `mac-min` for the trimmed set (recommended for deployment).

```bash
git clone --branch mac-full --single-branch --depth 1 https://github.com/joseluisbz/melt-binaries.git
```
