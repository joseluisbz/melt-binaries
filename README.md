# melt-binaries — win-min

Minimal working set of `melt.exe` for Windows — just enough to render an `.mlt` project to `.mp4` headlessly. See `main` branch for how this was determined.

```bash
git clone --branch win-min --single-branch --depth 1 https://github.com/joseluisbz/melt-binaries.git
```
