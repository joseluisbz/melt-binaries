# melt-binaries — win-full

Complete decompressed `shotcut-win64-26.8.1.zip` — melt.exe is at the root. See `main` branch for full repo layout, and `win-min` for the trimmed set (recommended for deployment).

```bash
git clone --branch win-full --single-branch --depth 1 https://github.com/joseluisbz/melt-binaries.git
```
